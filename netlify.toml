[build]
base = "website-gatsby"
publish = "public"
functions = "netlify/functions"
# Yarn 4 を Corepack でアクティブ化し、immutable縛りを外してインストール
command = """
corepack enable
corepack prepare yarn@4.4.0 --activate
node -v
yarn -v
yarn install
yarn build --verbose
"""

[build.environment]
NODE_VERSION = "20"
