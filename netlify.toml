[build]
base = "website-gatsby"
publish = "public"
command = "yarn install --frozen-lockfile && yarn build --verbose"

[build.environment]
NODE_VERSION = "18"

# (Functions を使うなら)
[functions]
directory = "netlify/functions"
