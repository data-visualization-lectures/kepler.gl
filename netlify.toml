[build]
# demo-app をローカルでビルドした成果物だけを配置する前提
base = "examples/demo-app"
publish = "dist"
command = "echo 'Use local build: netlify deploy --dir=examples/demo-app/dist'"

# Netlify 側でのビルド／依存インストールは行わないため、環境変数指定は不要

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
