[build]
  command = "corepack enable && corepack prepare yarn@4.4.0 --activate && node -v && yarn -v && yarn install --immutable && cd website-gatsby && yarn build --verbose"
  publish = "website-gatsby/public"

[build.environment]
  NODE_OPTIONS = "--max_old_space_size=4096"
