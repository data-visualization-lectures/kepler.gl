[build]
  command = "corepack enable && corepack prepare yarn@4.4.0 --activate && node -v && yarn -v && yarn install --immutable && cd website && yarn install --immutable && yarn build"
  publish = "website/public"

[build.environment]
  NODE_VERSION = "18"
  # MapboxAccessToken は Netlify UI で設定
